import React from "react";
import { Styles } from "./styles";

const Shop: React.FC = (...props) => {

  return (
    <Styles>
        <span>It's shop time</span>
    </Styles>
  );
};

export default Shop;
